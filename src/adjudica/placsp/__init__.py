"""PLACSP ATOM syndication: the Phase-1 document source.

Each feed entry's CODICE XML carries structured fields (the extraction ground truth) AND
direct pliego document links, in the same entry — so the XML-vs-PDF eval works within one
source, no cross-matching. Use ONLY this syndication feed, never the portal (constraint 3).
"""
